{
  "name": "@nos/shared-types",
  "version": "0.1.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -w -p tsconfig.json",
    "clean": "node -e \"fs.rmSync('dist', { recursive: true, force: true })\"",
    "typecheck": "tsc --noEmit",
    "lint": "eslint \"src/**/*.ts\" --fix"
  },
  "devDependencies": {
    "@nos/config-typescript": "workspace:*",
    "@nos/config-eslint": "workspace:*",
    "@types/node": "^22.10.1",
    "typescript": "^5.7.2"
  }
}
