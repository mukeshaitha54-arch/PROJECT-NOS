{
  "AgentConfiguration": {
    "ServerUrl": "http://localhost:3001",
    "_comment": "If DeviceId is empty, agent will auto-register on first run and store credentials in %LOCALAPPDATA%\\NOS\\",
    "DeviceId": "",
    "TenantId": "default-org",
    "ApiKey": "",
    "HeartbeatIntervalSeconds": 60,
    "TelemetryIntervalSeconds": 300,
    "InventoryIntervalSeconds": 3600,
    "SecurityScanIntervalSeconds": 900,
    "MaxCpuPercent": 15,
    "MaxRamMb": 256,
    "MaxConcurrentCollections": 2,
    "EnableOfflineQueue": true,
    "EnableSelfThrottling": true,
    "EnableWindowsEventLog": true,
    "LogRetentionDays": 7,
    "SqliteDbPath": "",
    "ResourceGuardrails": {
      "MaxCpuPercent": 3.0,
      "MaxRamMB": 128,
      "EmergencyRamMB": 200,
      "ThrottleTelemetryIntervalSec": 600,
      "ThrottleHeartbeatIntervalSec": 120
    }
  }
}
